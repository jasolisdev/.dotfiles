# 1080p (no scaling)
monitor=eDP-1,1920x1080@60,0x0,1

# 4K with 2x scale (uncomment to use native 4K)
# monitor=eDP-1,3840x2160@60,0x0,2

# Fallback for unknown monitors (kanshi handles known ones)
monitor=,preferred,auto,1
